def fib(n):  
    result = []
    a, b = 0, 1
    i=0
    while i < n:
        result.append(a)    
        a, b = b, a+b
        i+=1
    return result
n=int(input("Enter the no. of term: "))    
print(fib(n))
